import Header from "../features/Header/Header";
import Hero from "../features/Hero/Hero";

const Home = function () {
  return (
    <main>
      <Header />
      <Hero />
    </main>
  );
};

export default Home;
